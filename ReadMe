Dependencies:
1. Python3 and pip3 are required. To install them in linux use "sudo apt-get install python3". "sudo apt install python3-pip"
2. "Tabulate" package is required. To install it use the command "pip3 install tabulate --user".
   Some times tabulate requires the dependency "python3-setuptools".If so, use the command "sudo apt-get install -y python3-setuptools"to install it.


Instructions to execute project:
1. A Makefile is present in the same directory as project.
2. To run the project use the command "make run". This will take the input files and writes the output to result.txt
3. To clean up the temp files use the command "make clean". 
4. Input files can be modified by opening them normally.